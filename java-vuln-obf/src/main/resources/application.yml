server:
  port: 8081

spring:
  application:
    name: java-vuln-obf

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true

logging:
  level:
    root: INFO
    com.vulnobf: DEBUG
