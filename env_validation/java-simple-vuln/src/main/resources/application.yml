server:
  port: 8081

spring:
  application:
    name: java-simple-vuln

# 日志配置
logging:
  level:
    root: INFO
    com.envtest: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
